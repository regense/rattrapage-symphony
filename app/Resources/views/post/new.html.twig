{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter une VDM</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('post_index') }}">Retourner à la liste</a>
        </li>
    </ul>
{% endblock %}
